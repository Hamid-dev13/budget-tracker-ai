services:
  mcp:
    build: ./mcp
    volumes:
      - ./data:/app/data
    environment:
      - BUDGET_PATH=/app/data/budget.json

  dashboard:
    build: ./dashboard
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - BUDGET_PATH=/app/data/budget.json
    depends_on:
      - mcp
